import { List } from '@refinedev/antd'
import { Typography } from 'antd'

const { Title } = Typography

export const RecommendationPoolList = () => {
  return (
    <List>
      <Title level={4}>推荐池管理（即将上线）</Title>
    </List>
  )
}
